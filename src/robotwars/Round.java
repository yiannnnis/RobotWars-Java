/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package robotwars;

/**
 *
 * @author John
 */
public class Round {}/*
   private int curr_round;
   Room temp;
    
    public void nextRound(Colony colony)
    {
            for(int k=0; k<colony.numTunnels() ; k++)
            {
                   temp=colony.entryRooms[k];
                   for (int j=1 ; j<colony.tunnelLength(); j++) //Δωμάτιο
                   {
                       if(temp.getRobot()!=null)
                       {   
                           temp.getRobot().act(colony);
                       }
                       if(temp.getSoldier()!=null)
                       {
                           for(int i=0; i<temp.getSoldier().length; i++)
                           {
                               temp.getSoldier()[i].act(colony);                              
                           }
                       }
                       
                   }
            }
         setcurrRound(getcurrRound()+1);
        
    }
    
    public void setcurrRound(int curr_round)
    {
        this.curr_round=curr_round;
    }
    
    public int getcurrRound()
    {
        return curr_round;
    }
}*/